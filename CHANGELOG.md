# Changelog

## 0.9.3

- 修复 Windows 下分块上传初始化阶段遗留未关闭文件句柄，导致完成上传时 `.part` 重命名失败的问题。
- 修复连续本地上传与“附近设备”上传共同触发的 `The process cannot access the file because it is being used by another process`。
- 新增 Windows 专用回归测试，验证 upload init 后临时文件可以立即 rename/finalize。
- 保持 V0.9.2 的默认 HTTP 18888、UDP discovery 51889 及传输协议不变。

## 0.9.2

- 修复部分 Windows 环境中默认 TCP `51888` 位于动态/保留端口区间而导致启动后立即 `bind` 失败的问题；默认 HTTP 端口调整为 `18888`。
- UDP 设备发现端口继续保持 `51889`，保证 V0.9/V0.9.1/V0.9.2 设备仍可互相发现，并通过广播中的 HTTP 端口访问对应版本。
- HTTP 服务改为先 `net.Listen` 成功，再启动托盘、UDP discovery 和输出 `LAN Share ... started`，避免假启动日志。
- 新增已有实例识别：若目标端口已经运行 LAN Share，第二次启动会探测 `/healthz`、打开已有 Web UI 后正常退出。
- 非 LAN Share 的端口占用或系统端口保留会输出明确错误并提示使用 `-port <port>`。
- 新增已有实例探测单元测试；保持 V0.9.1 Responsive UI 与文件传输协议不变。

## 0.9.1

- 重构 Web UI 为标准响应式布局，桌面、1366、1024、平板和手机分辨率自动适配。
- 页面最大内容宽度提升到 1440~1480px，大屏减少无效留白，同时窄屏自动压缩边距与卡片尺寸。
- 左侧导航与顶部状态重新分工：去除重复网络/IP 状态，只在顶部保留连接状态，Sidebar 仅保留版本信息。
- 统一 Card、Button、Input、Table、Pagination、Dialog、Toast 的圆角、尺寸、边框和间距规范。
- 我的空间上传区更紧凑；共享文件列表在移动端自动切换为卡片式布局。
- 附近设备桌面布局调整为约 34% 设备列表 + 66% 共享文件，小屏自动变为上下排列。
- 最近传输在移动端自动切换为记录卡片，避免横向滚动和字段挤压。
- 设置页在桌面保持双栏、窄屏切换单列，配对码和操作按钮支持自动换行。
- 保持 V0.9 的分页、搜索、远程文件浏览、上传/下载、二维码、安全配对、大文件分块与 Range 路径不变。

## 0.9.0

- 按设计稿重构为固定左侧导航：我的空间、附近设备、最近传输、设置。
- 我的空间新增上传工作区、共享文件搜索/分页、下载、删除、复制临时下载链接、二维码展示。
- 附近设备改为双栏工作区：左侧设备列表，右侧按选中设备展示共享文件；支持远程下载和上传到指定设备。
- 远程共享文件支持分页和搜索，并保持对 V0.7/V0.8 设备的浏览兼容。
- 最近传输改为独立页面，支持分页、关键字、方向和状态筛选。
- 设置页面统一设备名称、共享目录、开机启动、安全模式、配对码和授权设备管理。
- `/api/files` 和 `/api/history` 增加可选分页响应，同时保留旧数组响应以兼容旧客户端。
- 新增分页/搜索单元测试；保持大文件分块、断点续传、Range、并行拉取和 AES-256-GCM 路径不变。

## 0.8.0

- 重构首页信息架构，突出快速发送、附近设备、我的共享和最近活动。
- 技术网络信息、安全配对、启动项和临时分享下沉到更多设置。
- 新增共享文件前端搜索和更友好的设备空状态。
- 保持 V0.7 传输协议和大文件性能路径不变。

## 0.7.0

- 新增设备卡片拖拽发送：文件可直接拖到目标设备，形成局域网 AirDrop 交互。
- 新增短时随机分享链接 `/s/<token>`，有效期 1 分钟～24 小时，可随时撤销。
- 新增纯 Go、零依赖的离线 QR Code（Version 5-L）生成器和 `/api/share-links/qr`。
- 分享链接在安全模式下仅放行单个文件，不绕过普通列表/上传授权。
- 分享链接下载继续支持 HTTP Range / 206，适用于大文件和断点下载。
- 新增 Windows Win32 原生托盘，保持 CGO=0 并可用 Go 1.20 构建 Win7 版本。
- 优化多网卡地址排序，优先常见物理 LAN 地址，降低 VPN/虚拟网卡被用作二维码地址的概率。
- 新增 QR、临时分享、安全隔离和 Range 下载端到端测试。

## 0.6.0

### V0.4 · Performance / large files

- 新增稳定 Upload ID + offset 协议，支持大文件分块上传和断点续传。
- Web UI 以 16 MiB chunk 顺序发送，失败自动重新查询 offset 并重试。
- 新增 4~8 路并行 Range 拉取远程已共享文件，目标端 `WriteAt` 并行落盘。
- 保留 `http.ServeContent(*os.File)` 下载热路径和 HTTP Range / 206。
- 增加实时速度、ETA、速度曲线和传输历史。
- 自动清理超过 7 天的孤立 `.part` 文件。

### V0.5 · Productization

- 设备名称可修改并持久化，UDP 广播实时更新。
- Windows / macOS / Linux 登录后自动启动支持。
- 最近 100 条发送/接收/拉取记录。
- 复制局域网地址、手动检查 GitHub Release 更新。
- CI 覆盖 Go 1.20.14 和 stable；Tag 自动构建 Release。

### V0.6 · Security

- 可选安全模式：未配对远程端禁止文件列表、下载和上传。
- 6 位配对码、失败尝试限流、信任设备白名单和撤销。
- Agent 配对采用 X25519 临时 ECDH + HMAC 配对证明。
- 配对 Token 通过 ECDH 派生密钥 + AES-GCM 封装返回，不直接明文返回。
- 已配对 Agent 的上传 chunk 使用 AES-256-GCM。
- 已配对 Agent 的并行 Range 拉取使用 AES-256-GCM。
- 远程浏览器支持配对后 HttpOnly Cookie 会话。
- 安全状态随 UDP 发现广播，UI 标注安全/配对状态。

## 0.3.0

- 修复 V0.2 源码与测试版本错位导致的构建失败问题。
- 重构为 `cmd/` + `internal/` 工程结构。
- 完整实现共享目录切换、校验和持久化。
- 管理类 API 限制为本机访问。
- 新增 `/healthz`、CI 和 Release 工作流。

## 0.2.0

- Web UI 增加共享目录切换。
- 共享目录持久化。
- 增加“打开目录”。

## 0.1.0

- 首个局域网文件互传 MVP。
